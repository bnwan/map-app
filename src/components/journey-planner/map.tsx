import * as React from 'react';

export interface IMapProps extends React.Props<any> {

}

export const Map = (props: IMapProps) => {

  return (
    <div className='map-container'>
      map container
    </div>
  );
};
